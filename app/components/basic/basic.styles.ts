import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    borderWidth: 1,
    borderColor: 'gray',
    margin: 10,
    padding: 10,
    gap: 5,
  },
  countText: {
    textAlign: 'center',
    fontSize: 20,
    marginBottom: 10,
  },
});
